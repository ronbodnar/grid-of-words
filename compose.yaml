version: "0.5"

services:
  app:
    container_name: word-puzzle-game
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      NODE_ENV: development
      MONGO_URI: ${MONGO_URI}
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
